import React from "react";
import { Box } from "@mui/material";

const Project = () => {
  return (
    <Box sx={{ border: "1px solid black", height: "7em", justifyItems: "end" }}>
      project
    </Box>
  );
};

export default Project;
